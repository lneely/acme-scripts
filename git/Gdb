#!/usr/bin/env rc
base=`{git merge-base HEAD origin/`{git symbolic-ref refs/remotes/origin/HEAD | sed 's|refs/remotes/origin/||'}}
if(~ $#samfile 0)
	git diff $base
if not
	git diff $base -- $samfile
